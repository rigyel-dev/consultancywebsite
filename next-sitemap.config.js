/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://www.rigyel.bt', // Replace with your domain
  generateRobotsTxt: true,
  sitemapSize: 5000,
  outDir: 'public',
};

module.exports = config;
